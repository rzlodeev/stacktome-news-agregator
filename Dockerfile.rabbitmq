# Custom Dockerfile for RabbitMQ service
FROM rabbitmq:3-management

# Optional: Add custom configurations, plugins, or scripts here
# Example: COPY rabbitmq_custom.conf /etc/rabbitmq/rabbitmq.conf

# Environment variables for default user and vhost
ENV RABBITMQ_DEFAULT_USER=myuser
ENV RABBITMQ_DEFAULT_PASS=mypassword
ENV RABBITMQ_DEFAULT_VHOST=myvhost

# Expose the necessary ports
EXPOSE 5672 15672

# Define the default command
CMD ["rabbitmq-server"]
